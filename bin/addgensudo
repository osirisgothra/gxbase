#!/bin/bash

[[ ${-//[^i]} ]] && { $BASH_SOURCE "$@"; return $?; }

[[ -d $GXBASE_ROOT ]] || { echo "GXBASE not loaded."; exit 1; }

for i in

if [[ -x $1 ]]; then
	echo "Automatically run $1 as a sudo command?"

if [[ -x $2 ]]
then
	echo "Hello no errors?"
fi


